---
type ShowcaseSection = {
  badge: string;
  badgeColor: string;
  heading: string;
  description: string;
  bullets: string[];
  image: string;
  imageAlt: string;
  layout: 'text-left' | 'image-left';
  cta?: { href: string; label: string };
};

const sections: ShowcaseSection[] = [
  {
    badge: 'Dashboard & Analytics',
    badgeColor: 'brand',
    heading: 'Your sub business at a glance',
    description:
      'Real-time KPIs built for specialty contractors — pipeline value, active jobs, pending bids, and crew activity. No GC overhead, just what matters to your trade.',
    bullets: [
      'Real-time project, revenue, and pipeline KPIs',
      'Activity feeds with team-wide visibility',
      'Pipeline tracking and project status overview',
    ],
    image: '/screenshots/01-dashboard.png',
    imageAlt:
      'BuildWorkPro dashboard with KPIs showing active projects, pipeline value, and revenue metrics',
    layout: 'text-left',
  },
  {
    badge: 'Sales Pipeline',
    badgeColor: 'emerald',
    heading: 'Never miss a bidding opportunity',
    description:
      'Track every opportunity from first call to signed contract. Drag-and-drop Kanban keeps your sales pipeline visible so you can focus on the jobs that matter.',
    bullets: [
      'Kanban board with drag-and-drop lead stages',
      'Deal values and probability tracking',
      'Stage-based pipeline with conversion analytics',
    ],
    image: '/screenshots/02-pipeline-kanban.png',
    imageAlt: 'Sales pipeline Kanban board showing leads organized by stage with deal values',
    layout: 'image-left',
  },
  {
    badge: 'Professional Bids & Estimates',
    badgeColor: 'violet',
    heading: 'Bid smarter, win more work',
    description:
      'Build detailed estimates with marks, labor rates, material costs, and overhead. Send polished proposals to GCs and convert accepted bids straight into active jobs.',
    bullets: [
      'Detailed line items with cost breakdown sidebar',
      'One-click PDF generation and client delivery',
      'Bid-to-project conversion on acceptance',
    ],
    image: '/screenshots/05-bid-detail-overview.png',
    imageAlt: 'Accepted bid detail showing line items, cost breakdown sidebar, and bid summary',
    layout: 'text-left',
  },
  {
    badge: 'Project Management',
    badgeColor: 'brand',
    heading: 'Run every job from mobilization to closeout',
    description:
      'Phases, tasks, Gantt charts, and crew assignments designed for how subs actually run jobs. Track progress across multiple active projects without losing visibility.',
    bullets: [
      'Phases and tasks with Gantt chart visualization',
      'Team assignment and workload tracking',
      'Progress tracking with milestone dependencies',
    ],
    image: '/screenshots/17-project-gantt.png',
    imageAlt: 'Project Gantt chart showing phases, tasks, and timeline with dependencies',
    layout: 'image-left',
  },
  {
    badge: 'Pay Applications',
    badgeColor: 'rose',
    heading: 'Stop chasing payments',
    description:
      'Submit progress billing pay apps to your GC with one click. Track retainage, change order impacts, and approval status so you always know where your money is.',
    bullets: [
      'Industry-standard schedule of values format',
      'Workflow approval: Draft, Submitted, Approved, Paid',
      'Retainage tracking and schedule of values',
    ],
    cta: { href: '/features/pay-applications/', label: 'AIA billing software' },
    image: '/screenshots/13-pay-app-detail.png',
    imageAlt:
      'Pay application detail showing workflow stepper, AIA summary, and schedule of values',
    layout: 'text-left',
  },
  {
    badge: 'Change Orders',
    badgeColor: 'amber',
    heading: 'Protect your margins on scope changes',
    description:
      'When the GC changes scope, capture it immediately. Track the contract impact, get approvals through a structured workflow, and make sure every dollar is accounted for.',
    bullets: [
      'Priced line items with add, modify, or remove changes',
      'Approved change orders adjust your contract value',
      'PDF generation to send for customer approval',
    ],
    image: '/screenshots/09-change-order-detail.png',
    imageAlt: 'Change order detail showing workflow stepper, line items, and contract impact',
    layout: 'image-left',
  },
  {
    badge: 'Site Logs',
    badgeColor: 'cyan',
    heading: 'Every jobsite day, documented',
    description:
      'When disputes arise, your site logs are your defense. Record personnel on site, work performed, materials received, visitors, and issues every day — with photos attached.',
    bullets: [
      'Daily field reports with structured data entry',
      'Tag entries: progress, issue, delay, safety, inspection',
      'Photo documentation and a manager-reviewed record',
    ],
    image: '/screenshots/11-site-log-detail.png',
    imageAlt: 'Site log detail showing personnel on site, work performed, and daily notes',
    layout: 'text-left',
  },
  {
    badge: 'Reports & Analytics',
    badgeColor: 'emerald',
    heading: 'Data-driven decisions for your business',
    description:
      'Reports with visual charts for pipeline health, bid conversion, project status, and team activity — so you know which work actually pays.',
    bullets: [
      'Pipeline and bid analytics with visual charts',
      'Project status and value breakdowns',
      'Export to CSV or PDF',
    ],
    image: '/screenshots/19-reports.png',
    imageAlt:
      'Reports dashboard showing bar charts and donut charts for pipeline and project analytics',
    layout: 'image-left',
  },
];

const badgeColorMap: Record<string, { bg: string; text: string; check: string }> = {
  brand: { bg: 'bg-brand-50', text: 'text-brand-700', check: 'text-brand-600' },
  emerald: { bg: 'bg-emerald-50', text: 'text-emerald-700', check: 'text-emerald-600' },
  violet: { bg: 'bg-violet-50', text: 'text-violet-700', check: 'text-violet-600' },
  amber: { bg: 'bg-amber-50', text: 'text-amber-700', check: 'text-amber-600' },
  rose: { bg: 'bg-rose-50', text: 'text-rose-700', check: 'text-rose-600' },
  cyan: { bg: 'bg-cyan-50', text: 'text-cyan-700', check: 'text-cyan-600' },
};
---

<section class="py-24 lg:py-32 bg-slate-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    {
      sections.map((section, index) => {
        const colors = badgeColorMap[section.badgeColor];
        const isImageLeft = section.layout === 'image-left';
        const isLast = index === sections.length - 1;

        return (
          <div
            class={`grid lg:grid-cols-2 gap-12 lg:gap-20 items-center ${isLast ? '' : 'mb-28 lg:mb-36'}`}
          >
            {/* Text content. Ordering applies at lg only: below the grid breakpoint the
                text must always precede its screenshot, or image-left sections stack two
                adjacent unlabeled screenshots between sections on mobile. */}
            <div class={isImageLeft ? 'lg:order-2' : ''}>
              <div
                class={`inline-flex items-center gap-2 ${colors.bg} ${colors.text} px-3 py-1 rounded-full text-sm font-medium mb-6`}
              >
                {section.badge}
              </div>
              <h3 class="text-3xl lg:text-4xl font-extrabold text-slate-900 tracking-tight mb-4">
                {section.heading}
              </h3>
              <p class="text-lg text-slate-500 mb-8 leading-relaxed">{section.description}</p>
              <ul class="space-y-4">
                {section.bullets.map((item) => (
                  <li class="flex items-start gap-3">
                    <svg
                      class={`w-5 h-5 ${colors.check} mt-0.5 shrink-0`}
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      stroke-width="2"
                    >
                      <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                    </svg>
                    <span class="text-slate-600">{item}</span>
                  </li>
                ))}
              </ul>
              {section.cta ? (
                <a
                  href={section.cta.href}
                  class="mt-8 inline-flex items-center gap-1.5 text-base font-semibold text-brand-600 hover:text-brand-700"
                >
                  {section.cta.label}
                  <svg
                    class="w-4 h-4"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                    stroke-width="2"
                    aria-hidden="true"
                  >
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
                  </svg>
                </a>
              ) : null}
            </div>

            {/* Screenshot */}
            <div class={isImageLeft ? 'lg:order-1' : ''}>
              <div class="rounded-2xl border border-slate-200/80 shadow-xl overflow-hidden bg-white">
                <img
                  src={section.image}
                  alt={section.imageAlt}
                  class="w-full h-auto block"
                  loading="lazy"
                />
              </div>
            </div>
          </div>
        );
      })
    }
  </div>
</section>
