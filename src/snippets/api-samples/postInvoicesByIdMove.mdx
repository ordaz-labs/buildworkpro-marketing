import { Tabs, TabItem } from "@astrojs/starlight/components";

<Tabs>
<TabItem label="curl">

```bash
curl -X POST https://app.buildworkpro.com/api/v1/invoices/{id}/move \
  -H "Authorization: Bearer ${API_KEY}"
```

</TabItem>
<TabItem label="JavaScript">

```js
const res = await fetch("https://app.buildworkpro.com/api/v1/invoices/{id}/move", {
  method: "POST",
  headers: {
    "Authorization": `Bearer ${process.env.BUILDWORKPRO_API_KEY}`,
  },
});
const data = await res.json();
```

</TabItem>
<TabItem label="Python">

```python
import os
import requests

res = requests.post(
  "https://app.buildworkpro.com/api/v1/invoices/{id}/move",
  headers={
    "Authorization": f"Bearer {os.environ['BUILDWORKPRO_API_KEY']}",
  },
)
data = res.json()
```

</TabItem>
</Tabs>
