node_modules
dist
.astro
.wrangler
# `public/` is served as-is by Astro — never reformat its contents
# (openapi.json, postman collection, robots.txt, images, etc.).
public
package-lock.json
demos
test-results
playwright-report
# Self-contained Playwright capture kit — drives the main app, not the site.
marketing-capture
# MDX files contain JSX-style comments ({/* ... */}) that Prettier's MDX
# parser mangles into invalid markdown escapes ({/\* ... \*/}). Skip them.
**/*.mdx

# Machine-local growth-review reports and sweep data (untracked, not part of the site)
.growth-review/
# generated by scripts/templates/build.mjs
src/data/templates-manifest/
tmp/
